import styled from 'styled-components';

export const ArrowRight = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: -5%;

  &:last-child {
    display: none;
  }
`;
